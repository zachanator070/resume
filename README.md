The resume of Zachary Johnson

HTML rendered version can be found at https://zachanator070.github.io/resume/

To build this site:
- install sphinx-build `sudo apt-get install python3-sphinx`
- build via make command `make all`